import {
  StyledCloseButton,
  StyledContainer,
} from "./ResourceViewControl.styled";

export const ResourceViewControl = () => {
  return (
    <StyledContainer>
      <p>ResourceViewControl</p>
      <StyledCloseButton />
    </StyledContainer>
  );
};
